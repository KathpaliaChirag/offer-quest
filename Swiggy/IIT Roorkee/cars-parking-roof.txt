Swiggy 
long carsPakingRoof(vector<int>cars,int k){
    
    sort(cars.begin(),cars.end());
    long  ans=LLONG_MAX,tmp,n=cars.size();
    for(int i=0;i<n-k+1;i++){
        tmp=cars[i+k-1]-cars[i]
        ans=min(ans,tmp+1)
    }
    return ans;
}

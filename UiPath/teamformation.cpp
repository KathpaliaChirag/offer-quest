#include<bits/stdc++.h>
using namespace std;
int main(){
	int n;
	cin>>n;
	vector<int> skills(n);
	int minplayers;
	int min_level;
	int max_level;



	/* approach to solve the problem

	sort the skills array 
	find the leftmost index of the for the min level
	find the rightmost index for the max level
	diff between them will give the length of possible.
	let length be len
	total=0
	for(i=min;i<=n;i++)
	then total+=C(len,i);
	where C denotes the combination.





	*/
	return 0;
}